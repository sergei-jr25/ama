/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'loremflickr.com',
			'picsum.photos',
			'avatars.githubusercontent.com',
			'cloudflare-ipfs.com',
		],
	},
}

module.exports = nextConfig
